.upcoming-compact-video
  = video_tag video.video, class: "upcoming-compact-video__thumnail"
  = link_to video_path(video), class: "upcoming-compact-video__video-link" do
    .upcoming-compact-video__video-link__video-title
      = video.title
    .upcoming-compact-video__video-link__video-owner
      = video.user.name
    .upcoming-compact-video__video-link__view-count
      = "視聴回数#{video.count}回"
